#include <iostream>
#include <string>

using namespace std;

void cutString(string s, int index){
    // TODO
    int i, n = s.length();
    string result = "";
    // for (i=0; i<n; +i){
    result = substr(index, n);
    // }
}